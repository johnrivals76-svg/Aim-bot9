-- Services
local Workspace = game:GetService("Workspace")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")

-- Local Player
local LocalPlayer = Players.LocalPlayer
local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
local HumanoidRootPart = Character:WaitForChild("HumanoidRootPart")
local Camera = Workspace.CurrentCamera

-- Settings
local Settings = {
    AimLockEnabled = true,
    AimLockActive = false, -- Controlled by touch input
    RainbowHighlightEnabled = true,
    FOVColor = Color3.new(1, 0, 0),
    FOVRadius = 15,
    SmoothAim = 100,
    Prediction = 100
}

-- Touch Input to Activate Aim Lock
UserInputService.TouchBegan:Connect(function(Touches, GameProcessedEvent)
    if GameProcessedEvent then return end -- Ignore if game already handled input (e.g., jumping)
    -- You can also check if touch is on a specific UI button
    Settings.AimLockActive = true
end)

UserInputService.TouchEnded:Connect(function(Touches, GameProcessedEvent)
    Settings.AimLockActive = false
end)

-- [Rest of the targeting/prediction/highlight logic remains the same as before]
-- Update main loop check: if not Settings.AimLockActive then return end

-- Main Loop
RunService.RenderStepped:Connect(function()
    if not Settings.AimLockEnabled or not Settings.AimLockActive then return end
    -- [Existing logic here]
end)
local FOVGui = Instance.new("ScreenGui")
FOVGui.Name = "FOVOverlay"
FOVGui.Parent = LocalPlayer.PlayerGui
FOVGui.IgnoreGuiInset = true -- Use full screen space

local FOVFrame = Instance.new("Frame")
FOVFrame.Name = "FOVIndicator"
-- Use scale so it stays proportional on all mobile screens
FOVFrame.Size = UDim2.new(0, 250, 0, 250) -- Slightly smaller for mobile
FOVFrame.Position = UDim2.new(0.5, -125, 0.5, -125)
FOVFrame.BackgroundTransparency = 1
FOVFrame.Parent = FOVGui

-- [Rest of FOV circle code same as before]
-- Main Window - Adjusted size for mobile
local MainWindow = Instance.new("Frame")
MainWindow.Name = "MainWindow"
MainWindow.Size = UDim2.new(0, 320, 0, 280) -- Compact size
MainWindow.Position = UDim2.new(0.05, 0, 0.1, 0)
MainWindow.BackgroundColor3 = Color3.new(0.1, 0.1, 0.1)
MainWindow.BorderColor3 = Color3.new(0.3, 0.3, 0.3)
MainWindow.BorderSizePixel = 2
MainWindow.Parent = MenuGui

-- Title Bar - Support touch dragging
local Dragging, DragStart, StartPos = false, nil, nil
TitleBar.InputBegan:Connect(function(Input)
    -- Support both mouse and touch
    if Input.UserInputType == Enum.UserInputType.MouseButton1 or Input.UserInputType == Enum.UserInputType.Touch then
        Dragging = true
        DragStart = Input.Position
        StartPos = MainWindow.Position
    end
end)

TitleBar.InputChanged:Connect(function(Input)
    if Dragging and (Input.UserInputType == Enum.UserInputType.MouseMovement or Input.UserInputType == Enum.UserInputType.Touch) then
        local Delta = Input.Position - DragStart
        MainWindow.Position = UDim2.new(
            StartPos.X.Scale,
            StartPos.X.Offset + Delta.X,
            StartPos.Y.Scale,
            StartPos.Y.Offset + Delta.Y
        )
    end
end)

TitleBar.InputEnded:Connect(function(Input)
    if Input.UserInputType == Enum.UserInputType.MouseButton1 or Input.UserInputType == Enum.UserInputType.Touch then
        Dragging = false
    end
end)

-- Controls - Larger hitboxes for touch
local AimLockToggle = Instance.new("TextButton")
AimLockToggle.Name = "AimLockToggle"
AimLockToggle.Size = UDim2.new(0, 180, 0, 44) -- Minimum touch size
AimLockToggle.Position = UDim2.new(0, 10, 0, 40)
AimLockToggle.BackgroundColor3 = Settings.AimLockEnabled and Color3.new(0, 1, 0) or Color3.new(1, 0, 0)
AimLockToggle.Text = "Aim Lock: " .. (Settings.AimLockEnabled and "ON" or "OFF")
AimLockToggle.TextColor3 = Color3.new(1,1,1)
AimLockToggle.TextSize = 16 -- Larger text
AimLockToggle.Parent = MainWindow
